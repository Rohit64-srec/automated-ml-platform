# This just to remind me some commands

Clear Cache
for /d /r . %d in (__pycache__) do @if exist "%d" rd /s /q "%d"

# View datasets
docker exec automl_postgres psql -U automl_user -d automl_dev -c "SELECT * FROM datasets;"

# View dataset versions
docker exec automl_postgres psql -U automl_user -d automl_dev -c "SELECT * FROM dataset_versions;"

# View ingestion jobs
docker exec automl_postgres psql -U automl_user -d automl_dev -c "SELECT * FROM ingestion_jobs;"

# Backend
cd apps/api && poetry run uvicorn app.main:app --reload --port 8000

# Terminal 2: Start the ARQ worker
cd apps/api && poetry run arq app.worker.WorkerSettings

# Frontend
cd apps/web && npm run dev